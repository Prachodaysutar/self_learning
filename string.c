#include<stdio.h>
#include<string.h>
int main()
{
    char string[]="just how long";
    int len;
    len = strlen(string);
    printf("given string :\n");
    puts(string);
    printf("is %d character long \n",len);
    return 0;

}
